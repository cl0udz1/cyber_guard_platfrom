# Cyber Guard Platform - Backend Environment Template
# Purpose: Document backend runtime configuration in one place.
# Inputs/Outputs: Copy to `backend/.env` and edit values for local/dev deployment.
# Dependencies: `app.core.config.Settings`.
# TODO Checklist:
# - [ ] Replace dev JWT secret before any real deployment.
# - [ ] Move secrets to a secure store (never commit `.env`).
# - [ ] Add environment-specific files if team uses staging/prod.
APP_NAME=Cyber Guard Platform API
APP_ENV=dev
APP_DEBUG=true
API_V1_PREFIX=/api/v1
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/cyber_guard
JWT_SECRET_KEY=CHANGE_ME_LOCAL_DEV_SECRET
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
VIRUSTOTAL_API_KEY=
VIRUSTOTAL_BASE_URL=https://www.virustotal.com/api/v3
HTTP_TIMEOUT_SECONDS=20
MAX_UPLOAD_SIZE_MB=10
CORS_ORIGINS=http://localhost:5173
DEMO_ORG_USER_PASSWORD=changeme123!

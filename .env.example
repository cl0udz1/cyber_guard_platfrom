# Cyber Guard Platform - Root Environment Template
# Purpose: Shared variables for docker-compose and local orchestration.
# Inputs/Outputs: Copy to `.env` in project root before using compose.
# Dependencies: `docker-compose.yml`, backend/frontend env templates.
# TODO Checklist:
# - [ ] Replace placeholder values for non-dev environments.
# - [ ] Store secrets securely (never commit real secrets).
POSTGRES_DB=cyber_guard
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

BACKEND_PORT=8000
FRONTEND_PORT=5173

LOCAL_AI_ENABLED=true
API_AI_ENABLED=true
PUBLIC_THREATS_API_ENABLED=false

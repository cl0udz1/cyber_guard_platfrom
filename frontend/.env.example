# Cyber Guard Platform - Frontend Environment Template
# Purpose: Configure frontend runtime values for API integration.
# Inputs/Outputs: Copy to `frontend/.env` for local dev.
# Dependencies: `src/api/client.ts`.
# TODO Checklist:
# - [ ] Point to deployed backend URL for demo/prod environments.
# - [ ] Add feature flags as project grows.
VITE_API_BASE_URL=http://localhost:8000

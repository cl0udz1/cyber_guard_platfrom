/// <reference types="vite/client" />

/**
 * Purpose:
 *   Type declarations for Vite environment variables (`import.meta.env`).
 * Inputs/Outputs:
 *   Consumed by TypeScript compiler for type-aware env access.
 * Dependencies:
 *   Vite type definitions.
 * TODO Checklist:
 *   - [ ] Add custom typed env variables when new frontend config keys are added.
 */

/**
 * Purpose:
 *   Thin re-export that keeps `main.tsx` stable while the app shell moves into `src/app`.
 * Inputs:
 *   None directly.
 * Outputs:
 *   Cyber Guard scaffold shell.
 * Dependencies:
 *   `src/app/AppShell.tsx`.
 */

export { default } from "./app/AppShell";




